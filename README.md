# Crypto coins app

## set the app up

1. clon the featur by git clone
   `git clone https://github.com/leib-ran/Cryptos_App.git`
2. install node packge mangement by
   `npm install`
3. open the progect by live server
